// 1. return a div with an id of "first" containing the string, "Hello, world"

// 2. This code is invalid because it attempts to return multiple root elements. Fix it so that one root element wraps the others:
return (
  <div id="second" className="exercise jsx">
    <label>Puppies</label>
    <ul>
      <li>Sasha</li>
      <li>Arnold</li>
      <li>Carmen</li>
    </ul>
  </div>
  <p>Who doesn't love puppies?</p>
  <h2>Puppies are the best</h2>
);

// 3. Use the variables, firstName and lastName, in the return statement, replacing 
export default (props) => {
  let firstName = 'Darla';
  let lastName = 'Rodriguez';
  return (

  );
}